import Auth from "./Auth";
const SignIn = () => {
  return (
    <div className="flex flex-col justify-center items-center min-h-screen">
      <Auth BtnValue="Sign In" />
    </div>
  );
};

export default SignIn;
